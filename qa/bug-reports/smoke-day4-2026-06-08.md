# Результаты smoke-прогона — День 4

| Поле | Значение |
|------|----------|
| **Дата** | 2026-06-08 (повторный прогон) |
| **Тестировщик** | QA |
| **Ветка** | `feature/qa-auth-setup` |
| **Окружение** | Windows 10, Docker Desktop 29.5.2 (engine недоступен), Node v22 |

---

## Итоговая таблица

| Пункт | Проверка | Статус | Комментарий |
|-------|----------|--------|-------------|
| **S1** | Docker поднимается без ошибок | ❌ **Fail** | `docker compose up` → 500 Internal Server Error (Docker Engine не запущен). См. BUG-002 |
| **S2** | `GET /api/health` → `{ status: "ok" }` | ✅ **Pass** | Локально `http://localhost:5000/api/health` → `{"status":"ok"}` |
| **S3** | Регистрация создаёт пользователя | ❌ **Fail** | `POST /api/auth/register` → **404** — маршрут не реализован. См. BUG-001 |
| **S4** | Логин возвращает токен | ❌ **Fail** | `POST /api/auth/login` → **404**. См. BUG-001 |
| **S5** | Страница логина открывается | ✅ **Pass** | `http://127.0.0.1:5173/login` → HTTP 200 |
| **S6** | После логина — переход на Dashboard | ⚠️ **Не проверено** | Фронт на mock-логине; нужна ручная проверка в браузере |
| **S7** | Без токена — редирект на `/login` | ⚠️ **Не проверено** | SPA-редирект в `PrivateRoute`; нужна ручная проверка |

**Общий результат:** ❌ **Fail**

---

## Блокеры

1. **Docker Engine** — после переустановки Docker Desktop демон не отвечает (`500` на API). Нужно запустить Docker Desktop и дождаться статуса Running.
2. **Auth API** — эндпоинты register/login/me не подключены в `backend/server.js`.

---

## Связанные баг-репорты

- [BUG-001 — Auth endpoints не реализованы](BUG-001-auth-endpoints-not-implemented.md)
- [BUG-002 — Docker Engine недоступен](BUG-002-docker-engine-not-running.md)
